import Header from '@/components/common/Header';

function MainPage() {
  return (
    <div className="min-h-screen bg-[var(--white-02)]">
      <Header />
      {/* 이후 다른 컴포넌트들을 여기에 추가 */}
    </div>
  );
}

export default MainPage;